# 密钥与环境
.env
llm_settings.json

# 运行时产物
uploads/
__pycache__/
*.pyc

# 工作流当前选择（运行时可变）
modules/training/workflow/current.json

# 数据库档位选择（运行时可变）
db_profile.json
.vendor/

# 本地数据备份（非源码，可能含生产数据，勿上传）
db/database/*.sql
db/*.sql
db/export/
*.bak.sql
*.bak*
data/*_bak_*.sql

# 仿真 skill 运行台账（脚本运行时生成：回滚清单/批量导入记录，非源码，含大体积 .purged）
*.purged
skills/deshu5-data-simulation/scripts/manifest/
# 仿真 skill 运行时状态指针（记录上次台账名，脚本自动重写，勿入库）
skills/deshu5-data-simulation/scripts/_lastman.txt

# 历史归档（清理移入的无用/测试文件，勿上传）
history/

# 外部工具记忆目录（自动再生）
.workbuddy/

# 前端工程（Vue 3 + Vite）：依赖与构建产物不入库
frontend/node_modules/
frontend/dist/

# 本地未入库物料（2026-09-11 F3.2 拍板显式声明；均为本地工作区留存，防误 add）
.DS_Store
app.log
app.pid
app_boot_test.log
演示汇报/
前端重构/
本地适配与修复记录.md
前端设计优化提示词.md

# 本地设计评审物料（2026-09-11 拍板：不入库，防误 add）
design-review/
design-review.rar
