# 旧前端（v1）归档

- **归档时间**：2026-09-11（F3.2 切换，皮卡丘拍板；来源=仓库根 `static/`，`git mv` 保留提交历史）
- **背景**：迁移并行期旧 UI 在 `/` 全程托管；F3.2 起新 UI（`frontend/`，Vue 3 + Vite）在 `/` 托管
- **状态**：本目录**不再对外服务**，仅供对照参照（F2 图谱等「原样复刻」的参照物）
- **回滚**：如需回滚至旧 UI，`git revert` F3.2 切换提交 + 重建前端 dist + 重启 Flask（详见 `前端重构/02-交接日志.md` 2026-09-11 F3.2 条目）
- 说明：归档为原样文件，内部 `/js/*`、`/css/*` 等绝对路径引用在归档语境下不再可解析（预期）
